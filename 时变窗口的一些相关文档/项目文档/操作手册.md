# GAP-Step 操作手册

## 1. 环境准备

```bash
cd /home/jack/wyh/时变窗口
source /home/jack/anaconda3/etc/profile.d/conda.sh
conda activate wyh
```

检查测试：

```bash
pytest -q
```

## 2. 当前入口

```bash
python -m gap_step.train
python -m gap_step.evaluate
python -m gap_step.visualize
```

当前只运行 GNN 特权教师，不运行旧视觉学生、BC、专家演示、`trainers/` 或 `scripts/`。

## 3. 训练

烟测：

```bash
python -m gap_step.train --config gap_step/configs/train_teacher_smoke.yaml
```

完整训练：

```bash
python -m gap_step.train --config gap_step/configs/train_teacher_full.yaml
```

训练顺序：

```text
C1 -> C1_5 -> C2A -> C2B -> C3 -> C4 -> C5
```

训练规则：

- 后一课程继承前一课程模型参数
- 每个课程重置优化器
- 每个课程只保存最终模型
- 不保存 `teacher_best.pt`
- 终端输出中文实时指标，不显示进度条
- PPO 使用显式旧策略：`model_old` 采样，`model` 更新，更新后同步

日志示例：

```text
课程 C1 | 更新 12 | 阶段步数 1536 | 回合 8 | 成功率 0.125 | 滚动成功率 0.100 | 碰撞率 0.750 | 超时率 0.125 | 平均回报 -8.42 | 动作范数 2.10 | 熵 2.91 | KL 0.0120 | 裁剪率 0.125 | 解释方差 0.500 | PPO更新 4
```

## 4. 输出位置

```text
checkpoints/C1/teacher_final.pt
results/C1/train_metrics.csv
checkpoints/C1_5/teacher_final.pt
results/C1_5/train_metrics.csv
...
checkpoints/C5/teacher_final.pt
results/C5/train_metrics.csv
```

## 5. 评估

评估最终课程模型：

```bash
python -m gap_step.evaluate --checkpoint checkpoints/C5/teacher_final.pt
```

只评估 C1：

```bash
python -m gap_step.evaluate --checkpoint checkpoints/C1/teacher_final.pt --episodes 20 --stages C1
```

用 C5 模型做全课程评估：

```bash
python -m gap_step.evaluate --checkpoint checkpoints/C5/teacher_final.pt --episodes 20 --stages C1,C1_5,C2A,C2B,C3,C4,C5
```

## 6. 可视化

```bash
python -m gap_step.visualize --checkpoint checkpoints/C5/teacher_final.pt
```

GIF 输出到 `results/`。

## 7. 当前重点

先确认 C1 是否稳定成功。C1 不稳定时，不要直接判断 C5 或 OOD 泛化，应先看：

- success rate
- collision rate
- timeout rate
- entropy
- std
- approximate KL
- clip fraction
- explained variance
- PPO 更新次数

## 8. 当前训练稳定性设置

```yaml
learning_rate: 0.0001
target_kl: 0.2
entropy_coef: 0.0001
log_std_init: -1.0
min_log_std: -2.0
max_log_std: 0.0
```

环境奖励设置中启用：

```yaml
reward_progress: 4.0
reward_timeout: -20.0
suppress_positive_progress_on_collision: true
```

含义：动态几何 progress 是主要 shaping；超时和碰撞一样是明确失败；撞墙或撞门时，不允许拿正的 progress reward。
