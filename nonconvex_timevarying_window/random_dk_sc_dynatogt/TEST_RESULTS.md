# Random-DK SC-DynaTOGT 实验记录

**多窗后续：** 三个自旋 U 窗口的开放赛道已运行 4000 候选，4 个通过全部三窗球体及顺序检查，但均超速，最终无合格解。见[多窗口记录](MULTI_WINDOW_RESULTS.md)。

**后续更新：** 同日按用户要求扩大 D/K 范围，并将预算增至每组 4000 候选后，两组各找到 1 条通过最终整机与动力学采样验收的轨迹，较快者 `T=3.167446933 s`。见[扩大范围实验](EXPANDED_SEARCH_RESULTS.md)。下文完整保留首次 300 候选的失败结果。

日期：2026-09-09；来源为本方法本地真实运行。方法及全局记忆变更尚未提交。两组使用相同的零厚度均衡 U、固定中心/平面、18 rad/s、初相位 1.1 rad 和动力学上限；随机种子均为 20260909，每组 300 个候选。

|SC 初始化|原始 T [s]|扰动候选|全部合格|随机搜索耗时 [s]|本次总耗时 [s]|
|---|---:|---:|---:|---:|---:|
|自身默认初值|2.455469423|300|0|0.292|23.642|
|Fixed-WP 最终解热启动|2.417043810|300|0|1.955|70.739|

两组原始 SC 优化器均正常停止，但原始轨迹动力学诊断均未通过：最大采样速度分别为 `8.456016893` 与 `8.687170798 m/s`，超过固定 `7 m/s` 上限。

默认初值组的 D 为约 `[7465.50,-5148.07]`，参数已非常靠近圆盘边界。随机候选中 299 个首先在 SC 安全多边形数值成员检查被淘汰，1 个通过球体筛选后因超速被淘汰。未通过成员检查的 299 个未继续计算球体或动力学，不能据此声称它们碰撞或动力学可行。

Fixed-WP 热启动组重新运行原 SC 后，D 为约 `[743.58,100.38]`；不能把此前旧运行中的 `[11.58,1.45]` 当作这次结果。300 个随机候选全部发现球体安全约束违规，立即淘汰，其余条件不作通过声明。

两组最终状态都是 `NO_FEASIBLE_CANDIDATE_FOUND`。没有合格候选，因此没有运行最终整机检测，也没有返回“违规最少”的替代轨迹。当前结果只说明这些局部邻域和 300 次预算未找到合格解；未证明不存在可行轨迹，也未证明扩大随机预算就能成功。

这两个搜索耗时不含 SC 求解和预处理；总耗时包含它们。未执行逐候选整机检测对照，不能给出加速比。所有检查都是采样数值验收，不是连续域认证。

## 实现验证

`conda wyh` 下本方法测试目录 **10 passed（30.21 s）**。覆盖直接 K/D 扰动范围与固定种子、三类候选数量、禁止不合格回退、原始解通过时不触发扰动、全部候选评估、多接触区间与重根/端点、真实 U 凹槽的实体距离、绝对时间旋转坐标、向量化 MINCO 求值与原生接口一致性，以及一个独立可行单窗夹具从球体/动力学筛选到最终整机检测的完整通过分支。该夹具只验证实现，不计入上面研究场景成功数。

两组各 301 行记录（原始解加 300 扰动）、连续候选编号、拒绝统计和无回退状态均核对通过；文档链接与上传约束的逐字节副本检查通过，`git diff --check` 通过。

## 完整产物

- [默认初值组报告](results/u_w18_seed20260909_300/REPORT.md)、[逐候选记录](results/u_w18_seed20260909_300/candidates.jsonl)、[完整结果](results/u_w18_seed20260909_300/result.json)。
- [Fixed-WP 热启动组报告](results/u_w18_fixed_warm_seed20260909_300/REPORT.md)、[逐候选记录](results/u_w18_fixed_warm_seed20260909_300/candidates.jsonl)、[完整结果](results/u_w18_fixed_warm_seed20260909_300/result.json)。

协议、场景多边形、模型参数、原始初值/最终 D/K、原始 MINCO 系数和每个候选的精确扰动都保存在各结果目录。两组独立目录保留，不覆盖旧实验。
